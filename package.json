{
  "name": "sudoku",
  "private": true,
  "version": "0.0.0",
  "homepage": "sudoku",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port=3000",
    "build": "tsc && vite build --base=/sudoku",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.1.0"
  }
}
